workflows:
  demo_build:
    name: Demo app build (hydration + APK + iOS simulator)
    max_build_duration: 90
    environment:
      flutter: stable
      groups:
        - Demo Free App Flow

    scripts:
      - name: ✅ Validate required vars (fail fast)
        script: |
          set -e
          [ -n "${MERCHANT_ID:-}" ] || (echo "❌ MERCHANT_ID is empty" && exit 1)
          [ -n "${PACKAGE_NAME:-}" ] || (echo "❌ PACKAGE_NAME is empty" && exit 1)
          [ -n "${APP_NAME:-}" ] || (echo "❌ APP_NAME is empty" && exit 1)

      - name: 🔎 Show CI variables
        script: |
          echo "MERCHANT_ID=$MERCHANT_ID"
          echo "PACKAGE_NAME=$PACKAGE_NAME"
          echo "APP_NAME=$APP_NAME"

      - name: 🧩 Hydrate (demo)
        script: |
          chmod +x ./hydrate_merchant.sh
          ./hydrate_merchant.sh "$MERCHANT_ID" "$PACKAGE_NAME" "$APP_NAME"

      - name: 🤖 Build Android APK (release)
        script: |
          flutter build apk --release

      - name: 🍎 Build iOS (simulator debug, no signing)
        script: |
          cd ios
          rm -rf Pods Podfile.lock
          pod repo update || true
          pod install
          cd ..
          flutter build ios --debug --simulator

  dedicated_build:
    name: Dedicated merchant build (hydration + APK + iOS simulator)
    max_build_duration: 90
    environment:
      flutter: stable
      groups:
        - Dedicated Merchant Paid Apps

    scripts:
      - name: ✅ Validate required vars (fail fast)
        script: |
          set -e
          [ -n "${MERCHANT_ID:-}" ] || (echo "❌ MERCHANT_ID is empty" && exit 1)
          [ -n "${PACKAGE_NAME:-}" ] || (echo "❌ PACKAGE_NAME is empty" && exit 1)
          [ -n "${APP_NAME:-}" ] || (echo "❌ APP_NAME is empty" && exit 1)

      - name: 🔎 Show CI variables
        script: |
          echo "MERCHANT_ID=$MERCHANT_ID"
          echo "PACKAGE_NAME=$PACKAGE_NAME"
          echo "APP_NAME=$APP_NAME"

      - name: 🧩 Hydrate (dedicated)
        script: |
          chmod +x ./hydrate_merchant.sh
          ./hydrate_merchant.sh "$MERCHANT_ID" "$PACKAGE_NAME" "$APP_NAME"

      - name: 🤖 Build Android APK (release)
        script: |
          flutter build apk --release

      - name: 🍎 Build iOS (simulator debug, no signing)
        script: |
          cd ios
          rm -rf Pods Podfile.lock
          pod repo update || true
          pod install
          cd ..
          flutter build ios --debug --simulator
